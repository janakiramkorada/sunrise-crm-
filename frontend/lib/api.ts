const api=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8080/api'; export type User={token:string;name:string;role:'ADMIN'|'MANAGER'|'EMPLOYEE'}; export async function request(path:string,options:RequestInit={}){const u=typeof window==='undefined'?null:localStorage.getItem('crmUser');const headers={"Content-Type":"application/json",...(u?{Authorization:`Bearer ${JSON.parse(u).token}`}:{}) ,...(options.headers||{})};const r=await fetch(`${api}${path}`,{...options,headers});if(!r.ok){const b=await r.json().catch(()=>({}));throw new Error(b.message||'Request failed');}return r.status===204?null:r.json();} export function current(){try{return JSON.parse(localStorage.getItem('crmUser')||'null')}catch{return null}}
